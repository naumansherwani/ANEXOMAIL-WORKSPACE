---
description: Four mail packages on anexomail.com — one AppShell, plan gate, no mix.
alwaysApply: true
---

# Package rail (locked)

Ek website. Teen hosts. Package = session `workspace_plan` (`plan-surface.ts`). `plans.ts` no-touch.

| Plan | Rail on `anexomail.com` |
|---|---|
| **Basic** | Dashboard · Mail · People · Calendar |
| **Pro** | + Work + CRM (own book) |
| **Business** | + Org + Chat + CRM Shared work |
| **Business Pro** | Business + CRM Activity ledger (audit, team seats) |

- URL se Chat/Work/Org/CRM/AI kholo to blank nahi — `PlanSurfaceGate` wall.
- LEO / AI Center **nahi** is host pe. AI = `ai.anexomail.com`.
- Founder / Speed / platform Admin = `founderworkspace.anexomail.com` only.
- Naya rail item tabhi jab us package ki card pe ho. Mix dump banned.
