---
description: Polar Rust payment is founder engine PM2 :3400. No-touch. Only check endpoints.
alwaysApply: true
---

# Polar Rust payment — no-touch

Founder ka payment engine: PM2 **`polar-rust-payment` :3400**. Kabza, **HTTP 200**. Is ko "dummy" mat bolo. Codes **no-touch**.

**Touch banned:** `server/rust/polar-payment/` (`main.rs` `Cargo.toml` `deploy.sh`) · `server/routes/polar.ts` webhook/bridge · Polar webhook SQL `sql/phase50*` `sql/phase51*` · Polar dashboard URLs · `.env` secrets · `plans.ts`.

Agent **sirf** yeh dekh sakta hai: endpoint sahi hai ya nahi. Engine rewrite / “fix Polar” / naya webhook **banned**.

## Endpoints (locked)

| Role | URL | Kahan |
|---|---|---|
| PRIMARY | `https://polarpayments.anexomail.com/api/v1/polar-webhook` | Caddy → `127.0.0.1:3400` |
| BACKUP | `https://anexomail.com/api/v1/polar-webhook` | Caddy → wahi `:3400` |
| Engine | `POST /api/v1/polar-webhook` on `:3400` | PM2 `polar-rust-payment` |
| Health | `http://127.0.0.1:3400/health` · `/ready` | gate only |

Bun `https://anexomail.com/api/public/polar/webhook` Polar dashboard mein **nahi**. Sirf forward `:3400`.

Personal vs Business kind / checkout land — Polar pe naya product ya engine change **nahi**. Success email recovery inbox pe Polar/engine se jati hai — yahan se mail mat likho.
