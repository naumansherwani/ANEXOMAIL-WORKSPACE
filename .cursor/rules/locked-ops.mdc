---
description: Follow AGENTS.md. No restore hacks. Language closed. E-series order.
alwaysApply: true
---

# Locked ops

Read `AGENTS.md` before code. Constitution: Hetzner, Caddy, React 19.2, TanStack Start, Vite 8.2, Tailwind 4.2, Bun :3000 SSR (frontend only). **Backend PRIMARY = Rust :3200 — async Tokio, WebTransport, QUIC.** Bun `:3100–3300` = **fallback total**. Naya phase Bun-primary **banned**. Polar rust payment `:3400` no-touch. **Vercel deploy this product pe nahi.** Compete = Zoho Mail look, generic SaaS nahi. Cards ki poori list ab implement **nahi**. Claude = mail UI only; backend agent = Rust/mail.

**Polar payment (locked):** Polar rust payment **`polar-rust-payment` PM2 :3400** (HTTP 200, no-touch). Agent sirf endpoint check: PRIMARY `https://polarpayments.anexomail.com/api/v1/polar-webhook` · BACKUP `https://anexomail.com/api/v1/polar-webhook` · engine `POST /api/v1/polar-webhook`. Bun `/api/public/polar/webhook` dashboard mein nahi.

**Server pull — sirf yeh (kuch add mat karo):**
```bash
cd /opt/anexomail-web && git pull && bun install && bun run build:bun && pm2 restart anexomail-web && pm2 save
```
`git restore src/routeTree.gen.ts` **banned** — user ko kabhi mat bolo. Generated tree build banati hai.

**Language (closed 11 Sep 2026):** Sign-in + landing English, har package. Picker sirf login ke baad workspace header. Founder account English. Is topic ko dubara mat kholo.

**SQL tracks (locked 11 Sep 2026) — mix nahi:**
- **Phase 60s = founder blueprint.** Touch **banned**: `sql/phase60*.sql` `sql/phase61*.sql` `sql/phase62*.sql` aur `docs/cursor-work/sql/phase63*.sql` `phase64*.sql` `phase65*.sql`. Rewrite / undo / dubara-run mat bolo. Fail ho to founder khud decide kare — agent file mat kaate.
- **Agent workflow = E-series** (`docs/cursor-work/ANEXOMAIL-EXECUTE.md`): E1 → E1A → E2 → E3 → **E4 mail gate** → E5–E8. Naya SQL sirf `docs/cursor-work/sql/E<n>_….sql`. `phase6Nb` / `_fix` / `_v2` banned.
- SQL fail = **usi E-file** theek. Phase 60s ko “fix” ke naam pe mat chhedo.
