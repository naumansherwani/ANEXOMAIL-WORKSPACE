# Wire — CRM cinematic + locale overlay

**Status: READY**

- Frontend: `/app/crm*` cinematic stage, CR0–CR12 strip, live `/api/crm/live` + memory + attach thread + evidence
- Brain: `server/routes/crm.ts` thread/evidence/graph writes; `server/routes/locale.ts` bundle + pref
- SQL: `docs/cursor-work/sql/phase64_crm_locale.sql` — founder paste

**DONE nahi** jab tak SQL run + server pull + live rows dikhen.
