# 02 — PHASE BY PHASE (har phase ka apna copy-paste command)

Pehle sirf ek dafa: `docs/wire/01-sql-all.md` ka **Step A** (DB connection).

Phir har phase ka block copy-paste karo. Output `GREEN <file>` aana chahiye.

Pull ek dafa:

```bash
cd /opt/anexomail-web && git pull
```

## 01. phase_leo_memory

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase_leo_memory.sql
```

## 02. phase12a_mail_prediction

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase12a_mail_prediction.sql
```

## 03. phase17_ai_studio

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase17_ai_studio.sql
```

## 04. phase18_ai_automation

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase18_ai_automation.sql
```

## 05. phase19_ai_billing

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase19_ai_billing.sql
```

## 06. phase20_ai_knowledge

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase20_ai_knowledge.sql
```

## 07. phase21_billing_platform

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase21_billing_platform.sql
```

## 08. phase22_integrations

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase22_integrations.sql
```

## 09. phase23_settings

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase23_settings.sql
```

## 10. phase24_analytics

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase24_analytics.sql
```

## 11. phase25_admin

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase25_admin.sql
```

## 12. phase26_security

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase26_security.sql
```

## 13. phase27_perf

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase27_perf.sql
```

## 14. phase28_handoff

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase28_handoff.sql
```

## 15. phase28_revenue

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase28_revenue.sql
```

## 16. phase30_release

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase30_release.sql
```

## 17. phase31_ai_credits

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase31_ai_credits.sql
```

## 18. phase32_trial

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase32_trial.sql
```

## 19. phase33_polar_checkout

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase33_polar_checkout.sql
```

## 20. phase34_billing_support

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase34_billing_support.sql
```

## 21. phase35_payment_safety

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase35_payment_safety.sql
```

## 22. phase36_state_sync

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase36_state_sync.sql
```

## 23. phase37_movein_ops

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase37_movein_ops.sql
```

## 24. phase38_movein_hardening — SIRF SQL EDITOR

Terminal se nahi chalti. File: `sql/editor/phase38_movein_hardening.sql`
(poora content copy karke SQL editor mein Run). Dekho `docs/wire/15-editor-sql.md`.

## 25. phase39_movein_fixes

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase39_movein_fixes.sql
```

## 26. phase40_evidence_truth

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase40_evidence_truth.sql
```

## 27. phase43_annual_billing_lock

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase43_annual_billing_lock.sql
```

## 28. phase44_polar_ids_v2

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase44_polar_ids_v2.sql
```

## 29. phase45_founder_identity

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase45_founder_identity.sql
```

## 30. phase46_pricing_v5

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase46_pricing_v5.sql
```

## 31. phase47_glitch_crm_alert

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase47_glitch_crm_alert.sql
```

## 32. phase48_storage_quota

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase48_storage_quota.sql
```

## 33. phase49_absm_billing_mesh

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase49_absm_billing_mesh.sql
```

## 34. phase49b_hotfix_random_token

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase49b_hotfix_random_token.sql
```

## 35. phase50_polar_rust_payment

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase50_polar_rust_payment.sql
```

## 36. phase51_polar_payment_hardening

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase51_polar_payment_hardening.sql
```

## 37. phase52_mail_launch

```bash
cd /opt/anexomail-web && bash sql/run.sh sql/phase52_mail_launch.sql
```

## 39. anexochat_phase01_foundation — SIRF SQL EDITOR

Terminal se nahi chalti. File: `sql/editor/anexochat_phase01_foundation.sql`
(poora content copy karke SQL editor mein Run). Dekho `docs/wire/15-editor-sql.md`.

## 40. anexochat_phase03_message_engine

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/anexochat_phase03_message_engine.sql
```

## 41. anexochat_phase07_cinema_video

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/anexochat_phase07_cinema_video.sql
```

## 42. anexochat_phase10a_call_engine

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/anexochat_phase10a_call_engine.sql
```

## 43. anexochat_phase10b_8k_video

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/anexochat_phase10b_8k_video.sql
```

## 44. anexochat_phase11_attachments

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/anexochat_phase11_attachments.sql
```

## 45. anexochat_phase11b_attachment_flag

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/anexochat_phase11b_attachment_flag.sql
```

## 46. anexochat_phase12_continuity

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/anexochat_phase12_continuity.sql
```

## 47. phase13_15_file_engine

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase13_15_file_engine.sql
```

## 48. phase16_18_file_truth_safety

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase16_18_file_truth_safety.sql
```

## 49. phase19_22_device_safety_work

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase19_22_device_safety_work.sql
```

## 50. phase23_promise_engine

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase23_promise_engine.sql
```

## 51. phase24_decision_ledger

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase24_decision_ledger.sql
```

## 52. phase24a_account_integrity

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase24a_account_integrity.sql
```

## 53. phase25_27_timeline_health_provenance

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase25_27_timeline_health_provenance.sql
```

## 54. phase28_receipts

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase28_receipts.sql
```

## 55. phase29_email_to_chat

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase29_email_to_chat.sql
```

## 56. phase30_chat_to_email

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase30_chat_to_email.sql
```

## 57. phase31_file_context

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/phase31_file_context.sql
```

## 58. videocall_phase31_call_record

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/videocall_phase31_call_record.sql
```

## 59. videocall_phase31a_lightspeed

```bash
cd /opt/anexomail-web && bash sql/run.sh anexochat/sql/videocall_phase31a_lightspeed.sql
```

## Aakhir mein verify

```bash
cd /opt/anexomail-web && bash sql/verify.sh
```
